import React, {useState, useEffect} from 'react';
import MapContainer from '../../components/GalaxyMap/MapContainer';
import styles from './Map.module.sass';

const Map: React.FC = () => <MapContainer />;

export default Map;
