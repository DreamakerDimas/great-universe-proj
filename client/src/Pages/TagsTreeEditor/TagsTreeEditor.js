import React from 'react';
import styles from './TagsTreeEditor.module.sass';

// only for admin, moderator !!!
function TagsTreeEditor(props) {
  return (
    <div className={styles.mainContainer}>
      {/* loader */}
      {/* tagsEditor */}
      <div>Tags Editor</div>
    </div>
  );
}

export default TagsTreeEditor;
